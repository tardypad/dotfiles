# start initial browser
exec chromium

# Projects terminal
exec termite \
  --exec tmux \
  --title sway_projects

# Servers terminal
exec termite \
  --title sway_servers

# Chat terminal
exec termite \
  --exec weechat \
  --title sway_chat
