# start initial browser
exec chromium

# Projects terminal
exec termite \
  --exec 'sh -c "TERM=xterm-256color tmux"' \
  --title sway_projects

# Servers terminal
exec termite \
  --title sway_servers

# Chat terminal
exec termite \
  --exec weechat \
  --title sway_chat
