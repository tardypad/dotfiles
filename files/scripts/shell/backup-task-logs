#!/bin/sh

LOCAL_TASK_LOGS_FOLDER="${XDG_DATA_HOME:-$HOME/.local/share}/tasks"
REMOTE_TASK_LOGS_FOLDER="backup/$( uname -n )/tasks"

rsync -azb --delete --exclude=".*" \
  "${LOCAL_TASK_LOGS_FOLDER}/" \
  surg:"${REMOTE_TASK_LOGS_FOLDER}"
