#!/bin/sh
# commands used:
# - task

TASK=$( task show 2> /dev/null )

[ -n "${TASK}" ] || exit 0

COLOR='#{color/info/hex}'
TEXT=$( echo "${TASK}" | cut -f1 )

printf '%s\n\n%s\n' "${TEXT}" "${COLOR}"
