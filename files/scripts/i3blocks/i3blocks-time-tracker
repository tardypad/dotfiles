#!/bin/sh

TRACKING_STATUS=$( time-tracker status 2> /dev/null )

[ -n "${TRACKING_STATUS}" ] || exit 0

TASK_NAME=$( echo "${TRACKING_STATUS}" | cut -f 2 )

echo "<span foreground='#{color/notice/hex}'>  ${TASK_NAME}</span>"
