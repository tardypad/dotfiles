# commands used:
# - sum
# - time-tracker

# display sum of tracked hours reported for a day

local day="$1"

local total

total=$(
  time-tracker -t overview "$day" \
    | cut -f 2 \
    | sum
)

[ "$?" -ne 0 ] && return 1

printf '%.1f\n' $( echo "${total} / 60" | bc -l )
