# commands used:
# - sum
# - task

# display sum of task hours reported for a day

local day="$1"

local total

total=$(
  task -t overview "$day" \
    | cut -f 2 \
    | sum
)

[ "$?" -ne 0 ] && return 1

printf '%.1f\n' $( echo "${total} / 60" | bc -l )
