#compdef backup

_sources() {
  local sources=(
    laptop
  )

  compadd "$@" $sources
}

_targets() {
  local targets=(
    hdd
    server
  )

  compadd "$@" $targets
}

_items() {
  local source='laptop' target='hdd' items=()

  for (( i = 1; i <= $#words - 1; i++ )); do
    if [ $words[$i] = '-s' ]; then
      source=$words[$((i+1))]
    elif [ $words[$i] = '-t' ]; then
      target=$words[$((i+1))]
    fi
  done

  if [ $source = 'laptop' ]; then
    if [ $target = 'hdd' ]; then
      items=(
        bookmarks
        books
        contacts
        notes
        videos
      )
    elif [ $target = 'server' ]; then
      items=(
        bookmarks
        contacts
        notes
        time_tracker_logs
      )
    fi
  fi

  compadd "$@" $items
}

_arguments \
  '-s[source]:source:_sources' \
  '-t[target]:target:_targets' \
  '1:item:_items'
