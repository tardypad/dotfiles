all: libqalculate-configs

libqalculate-configs:
	$(start_setup)

	mkdir -p $(DESTDIR)$(CONFIG_DIR)/qalculate

	$(setup_configs_file)/libqalculate/qalc.cfg > $(DESTDIR)$(CONFIG_DIR)/qalculate/qalc.cfg
