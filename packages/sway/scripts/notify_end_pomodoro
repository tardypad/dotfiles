#!/usr/bin/env zsh

TMP_RESULT_FILE=$(mktemp)

cat << EOF > "${TMP_RESULT_FILE}"

Take a break
EOF

notification 'Pomodoro end' "${TMP_RESULT_FILE}"

aplay "${XDG_CONFIG_HOME:-$HOME/.config}/sway/sounds/end_pomodoro.wav"
