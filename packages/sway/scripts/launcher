#! /usr/bin/env zsh

COMMAND="$@"

# try to focus existing window if it exists
focus_window_title 'sway_launcher' && exit

# launch new launcher otherwise
swaymsg exec \
  "termite \
   --exec \"${COMMAND}\" \
   --config ${HOME}/.config/termite/config_launcher \
   --title sway_launcher"
