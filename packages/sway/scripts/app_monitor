#!/usr/bin/env zsh

TITLE='sway_monitor'

# try to focus existing window if it exists
focus_window_title "${TITLE}" && exit

# launch new app otherwise
swaymsg --quiet exec \
  "termite \
   --title \"${TITLE}\" \
   --exec 'tmux new-session -A -s monitor \"htop\"'"
