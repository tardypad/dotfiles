#!/bin/sh

printf '%s' "${PATH}" \
  | tr ':' '\n' \
  | xargs -I [] \
    -- find -L [] \
       -type f \
       -exec basename {} \; \
       2> /dev/null \
  | sort -u \
  | fzf_launcher 'run ' \
  | xargs -I{} \
    -- swaymsg -q exec {}
