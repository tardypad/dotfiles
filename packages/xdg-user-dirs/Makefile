xdg-user-dirs:
	$(start_setup)

	$(call copy_file,user-dirs.dirs,$(CONFIG_DIR)/user-dirs.dirs)
	$(call copy_file,user-dirs.conf,$(CONFIG_DIR)/user-dirs.conf)
	$(call copy_file,user-dirs.locale,$(CONFIG_DIR)/user-dirs.locale)

	$(end_setup)
