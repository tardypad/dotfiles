#!/bin/sh

FILE_PATH="${XDG_RUNTIME_DIR}/chat_count"

[ -f "${FILE_PATH}" ] || exit 0

COUNT=$( cat "${FILE_PATH}" )

[ "${COUNT}" -gt 0 ] || exit 0

echo "<span foreground='#{color/notice/hex}'> ${COUNT}</span>"
