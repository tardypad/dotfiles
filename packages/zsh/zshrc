# load all individual configs
# plugins with config need to be loaded at the end
for conf in "${HOME}/.zsh/config.d/"*.zsh; do
  [[ ${conf} =~ '.*/plugins.zsh' ]] && continue
  source "${conf}"
done
unset conf

source "${HOME}/.zsh/config.d/plugins.zsh"
