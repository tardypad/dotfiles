# Display the full path of a project logs folder
#
# Display nothing given no argument or invalid one

local project_name="$1"

find "$( xdg-user-dir PROJECTS_LOGS )"/* \
  -type d -prune \
  -name "${project_name}"
