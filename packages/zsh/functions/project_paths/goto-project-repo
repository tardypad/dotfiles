# Change current directory to a project repository folder
#
# Use projects main folder given no argument

if [[ -z "$1" ]]; then
  cd "$( xdg-user-dir PROJECTS )"
  return
fi

local repo_path=$( get-project-repo-path "$1" )

if [[ -n "${repo_path}" ]]; then
  cd "${repo_path}"
  git current-branch
else
  echo "No such project repository"
fi
