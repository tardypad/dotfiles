# Display the full path of a project main folder
#
# Display nothing given no argument or invalid one

local project_name="$1"

find "$( xdg-user-dir PROJECTS )" \
  -mindepth 1 -maxdepth 1 -type d \
  -name "${project_name}"
