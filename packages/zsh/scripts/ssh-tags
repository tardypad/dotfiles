#!/bin/sh

# #IDEA: add fzf preview of tags to help selection of right host
SELECTED_HOST=$(
  query-hosts-tags tags "$@" \
    | fzf \
      --no-multi \
      --exit-0 \
      --select-1 \
      --layout=reverse
)

[ -n "${SELECTED_HOST}" ] || exit

ssh "${SELECTED_HOST}"
