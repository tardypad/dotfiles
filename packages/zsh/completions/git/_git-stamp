#compdef git-stamp
#description amend last commit with a stamp message prefixed by an identifier

_arguments \
  '-h[display the help]' \
  '(-r --replace)'{-r,--replace}'[replace all previous stamps with same id]'
