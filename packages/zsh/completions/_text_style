#compdef text_style

_names() {
  local names=(
    upside_down
    full_width
    mocking_spongebob
  )

  compadd "$@" $names
}

_arguments \
  '(-h --help)'{-h,--help}'[display the help]' \
  '1:name:_names'
