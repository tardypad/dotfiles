#compdef goto-project-main

_main_projects() {
  local main_projects=( $(
    find "$( xdg-user-dir PROJECTS )"/* \
      -type d -prune \
      -exec basename {} \;
  ) )

  compadd "$@" $main_projects
}

_arguments \
  '1:main project:_main_projects'
