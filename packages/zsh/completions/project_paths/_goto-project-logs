#compdef goto-project-logs

_logs_folders() {
  local logs_folders=( $(
    find "$( xdg-user-dir PROJECTS_LOGS )"/* \
      -type d -prune \
      -exec basename {} \;
  ) )

  compadd "$@" $logs_folders
}

_arguments \
  '1:logs folder:_logs_folders'
