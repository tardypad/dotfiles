#compdef goto-project-repo

_projects_repos() {
  local projects_repos=( $(
    find "$( xdg-user-dir PROJECTS )"/*/* \
      -type d -prune \
      -exec basename {} \;
  ) )

  compadd "$@" $projects_repos
}

_arguments \
  '1:project repo:_projects_repos'
