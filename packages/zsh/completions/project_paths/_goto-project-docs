#compdef goto-project-docs

_docs_folders() {
  local docs_folders=( $(
    find "$( xdg-user-dir PROJECTS_DOCS )"/* \
      -type d -prune \
      -exec basename {} \;
  ) )

  compadd "$@" $docs_folders
}

_arguments \
  '1:docs folder:_docs_folders'
