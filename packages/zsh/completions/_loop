#compdef loop

_arguments \
  '-c[stops after COUNT runs of the command]:()' \
  '-h[display the help]' \
  '-i[wait INTERVAL seconds between each command run]:()' \
  '-t[stops commands run after a TIME seconds period]:()'
