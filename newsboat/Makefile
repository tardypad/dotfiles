newsboat:
	$(start_setup)

	$(call copy_file,urls,$(CONFIG_DIR)/newsboat/urls)
	$(call copy_file,config,$(CONFIG_DIR)/newsboat/config)
	$(call copy_folder,config.d,$(CONFIG_DIR)/newsboat/config.d)

	$(end_setup)
