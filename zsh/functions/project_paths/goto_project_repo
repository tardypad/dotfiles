# Change current directory to a project repository folder
#
# Use projects main folder given no argument or invalid one

local repo_path=$( get_project_repo_path "$1" )

if [[ -n "${repo_path}" ]]; then
  cd "${repo_path}"
else
  cd "$( xdg-user-dir PROJECTS )"
fi
