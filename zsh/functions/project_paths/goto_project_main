# Change current directory to a project main folder
#
# Use projects main folder given no argument or invalid one

local main_path=$( get_project_main_path "$1" )

if [[ -n "${main_path}" ]]; then
  cd "${main_path}"
else
  cd "$( xdg-user-dir PROJECTS )"
fi
