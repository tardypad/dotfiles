# Change current directory to a project documents folder
#
# Use projects main documents folder given no argument or invalid one

local docs_path=$( get_project_docs_path "$1" )

if [[ -n "${docs_path}" ]]; then
  cd "${docs_path}"
else
  cd "$( xdg-user-dir PROJECTS_DOCS )"
fi
