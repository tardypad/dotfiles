#compdef emoji_animate

_names() {
  local names=(
    fliptable
    cool
    shrug
    disapprove
    happygary
  )

  compadd "$@" $names
}

_arguments \
  '(-h --help)'{-h,--help}'[display the help]' \
  '1:name:_names'
