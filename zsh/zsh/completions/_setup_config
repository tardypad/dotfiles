#compdef setup_config

_tools() {
  local tools=( $(
    find . \
      -maxdepth 1 -type d \
      ! -name '.*' \
      -printf '%f '
  ) )

  compadd "$@" $tools
}

_arguments \
  '(-h --help)'{-h,--help}'[display the help]' \
  '*:tools:_tools'
